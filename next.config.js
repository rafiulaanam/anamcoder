/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "img.icons8.com",
      "www.rlogical.com",
      "upload.wikimedia.org",
      "i.ibb.co"
    ], // Add the hostname here
  },
};

module.exports = nextConfig;
